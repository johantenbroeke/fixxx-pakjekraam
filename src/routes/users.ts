import { Request, Response } from 'express';


export const deleteUser = (req: Request, res: Response, erkenningsNummer: string) => {
    
    
    
};

export const deleteUserPage = ( req: Request, res: Response ) => {

    return res.render('DeleteUserPage',{});

};
